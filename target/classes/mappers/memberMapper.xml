<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.memberMapper">

	<!-- mybatis-config.xml의 typeAlias에 등록해두지 않았다면
	<select id="selectMember" resultType="com.example.dto.MemberVO" >
		SELECT ID, PW ,NAME FROM TEST
	</select> 
		이렇게 해야 한다. -->

	<select id="selectMember" resultType="MemberModel">
		SELECT USER_ID, USER_PW , USER_NAME, USER_ADD, USER_ADD, USER_ADD_CODE, IF('IS_ACTIVE', 'true', 'false') FROM USER
	</select>

	<select id="selectMemberLogin" resultType="MemberModel">
		SELECT USER_ID, USER_PW , USER_NAME, USER_ADD, USER_ADD, USER_ADD_CODE, IF('IS_ACTIVE', 'true', 'false') FROM USER WHERE USER_ID = #{user_id}
	</select>
	
</mapper>